<html>
<head>
    <title>Curso Node</title>
    {{> css_javascript}}
</head>
<body>
<div class="container">
    {{> menu}}
    <h2>Proceso Inscripción</h2>
    <hr/>

    {{#if existeUsuarioCurso}}
    <div class="alert alert-danger" role="alert">
        El usuario ya ha realizado una inscripción a este curso.
    </div>
    {{/if}}

    {{#if guardado}}
        <div class="alert alert-success" role="alert">
            La inscripción se guardó correctamente.
        </div>
    {{/if}}

    {{#if error}}
        <div class="alert alert-danger" role="alert">
            Error al guardar el registro.
        </div>
    {{/if}}

    <div class="row">
        <div class="col-md-12 mx-auto">
            <form action="/guardar-proceso-inscripcion" method="post">
                <div class="form-group row">
                    <div class="col-sm-6">
                        <label for="curso">Curso * </label>
                        <select class="form-control" id="idCurso" name="idCurso">
                            {{#each cursos}}
                                <option value="{{idCurso}}">{{nombre}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <div class="col-sm-6">
                        <label for="nombre">Documento Identidad * </label>
                        <input type="text" class="form-control" id="documento" name="documento" placeholder="Documento Identidad" required value="{{usuario.documento}}">
                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-sm-6">
                        <label for="nombre">Nombre * </label>
                        <input type="text" class="form-control" id="nombre" name="nombre" placeholder="Nombre" required value="{{usuario.nombre}}">
                    </div>
                    <div class="col-sm-6">
                        <label for="nombre">Correo * </label>
                        <input type="text" class="form-control" id="correo" name="correo" placeholder="Correo" value="{{usuario.correo}}">
                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-sm-6">
                        <label for="modalidad">Teléfono * </label>
                        <input type="text" class="form-control" id="telefono" name="telefono" placeholder="Teléfono" value="{{usuario.telefono}}">
                    </div>
                    <div class="col-sm-6">
                        &nbsp;
                    </div>
                </div>
                <button type="submit" class="btn btn-primary px-4 float-right">Guardar</button>
            </form>
        </div>
    </div>
</div>
</body>
</html>
